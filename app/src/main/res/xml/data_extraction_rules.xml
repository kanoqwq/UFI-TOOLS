<?xml version="1.0" encoding="utf-8"?>
<!--
  这个应用的 SharedPreferences 里存着登录口令哈希、路由器后台密码、
  SMTP 密码和钉钉密钥，而且口令哈希本身就是 API 的 bearer 凭据。
  一旦随备份导出，等于把设备控制权一起导出去了。
  所以 cloud-backup 和 device-transfer 全部排除。
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
